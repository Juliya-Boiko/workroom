export enum EIconsSet {
  Checkbox,
  ArrowDown,
  ArrowRight,
  ArrowLeft,
  Eye,
  Plus,
  Settings,
  Logout,
  Dashboard,
  Calendar,
  Persons,
  Projects,
  Bell,
}

export enum EIconPosition {
  Append,
  Prepend,
}